use crate::context::Context;
use progenitor_client::{Error, ResponseValue};

// The MVP keeps CliConfig in print.rs to keep rendering concerns isolated;
// oxide.rs implements the same trait in main.rs, and either placement compiles.
impl {{ progenitor_crate_name }}::CliConfig for Context {
    fn success_item<T>(&self, value: &ResponseValue<T>)
    where
        T: schemars::JsonSchema + serde::Serialize + std::fmt::Debug,
    {
        println!("{}", serde_json::to_string_pretty(value).unwrap());
    }

    fn success_no_item(&self, value: &ResponseValue<()>) {
        println!("{}", serde_json::to_string_pretty(value).unwrap());
    }

    fn error<T>(&self, value: &Error<T>)
    where
        T: schemars::JsonSchema + serde::Serialize + std::fmt::Debug,
    {
        eprintln!("{}", serde_json::to_string_pretty(value).unwrap());
        std::process::exit(1);
    }

    fn list_start<T>(&self)
    where
        T: schemars::JsonSchema + serde::Serialize + std::fmt::Debug,
    {
    }

    fn list_item<T>(&self, value: &T)
    where
        T: schemars::JsonSchema + serde::Serialize + std::fmt::Debug,
    {
        println!("{}", serde_json::to_string_pretty(value).unwrap());
    }

    fn list_end_success<T>(&self)
    where
        T: schemars::JsonSchema + serde::Serialize + std::fmt::Debug,
    {
    }

    fn list_end_error<T>(&self, value: &Error<T>)
    where
        T: schemars::JsonSchema + serde::Serialize + std::fmt::Debug,
    {
        eprintln!("{}", serde_json::to_string_pretty(value).unwrap());
        std::process::exit(1);
    }
}
